# chip-8 emulator in rust

libchip8 todo:

-   opcodes 8XY1 through 8XYE, 9XY0, BNNN, EX9E through FX65
-   inaccurate behaviour in DXYN
-   clean up integer types (u16, u32, usize mixup)

f/e app todo:

-   make a real app to run it
-   load ROMs properly
-   wasm support?
